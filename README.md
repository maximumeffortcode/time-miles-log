# Time & Miles Log

A Streamlit app for logging start/end miles and start/end times, and exporting date-based PDF reports.

## Features
- Manual start/end mileage entry
- Start/end time tracking
- Date range filtering
- PDF export with quick date presets
- Password-protected access

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
